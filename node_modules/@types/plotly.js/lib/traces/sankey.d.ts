import { PlotData } from '../../index';

export interface SankeyData
    extends PlotData {
    type: 'sankey';
    node: {
        pad: number,
        thickness: number,
        line: {
            color: string,
            width: number,
        },
        label: string[],
        color: string[],
    },
    link: {
        source: number[],
        target: number[],
        value: number[],
    }
}